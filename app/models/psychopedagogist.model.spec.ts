import { Psychopedagogist } from './psychopedagogist.model';

describe('Psychopedagogist', () => {
  it('should create an instance', () => {
    expect(new Psychopedagogist()).toBeTruthy();
  });
});
