import { AppointmentRequest } from './appointment-request.model';

describe('AppointmentRequest', () => {
  it('should create an instance', () => {
    expect(new AppointmentRequest()).toBeTruthy();
  });
});
