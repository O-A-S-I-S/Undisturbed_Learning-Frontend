import { AppointmentFilter } from './appointment-filter.model';

describe('AppointmentFilter', () => {
  it('should create an instance', () => {
    expect(new AppointmentFilter()).toBeTruthy();
  });
});
